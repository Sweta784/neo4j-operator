apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "neo4j-operator.fullname" . }}-test-connection"
  labels:
    app: "{{ include "neo4j-operator.name" . }}"
spec:
  containers:
  - name: curl
    image: curlimages/curl
    command: ['curl']
    args:  ['neo4j-operator:7474']
